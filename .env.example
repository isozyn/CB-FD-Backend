# Environment Configuration for Railway Deployment
# Copy this file to .env for local development
# Railway will inject these variables automatically

# Application Environment
NODE_ENV=production

# Server Configuration
PORT=3000

# Frontend URL (Replace with your actual Netlify site URL)
NETLIFY_SITE_URL=https://your-site-name.netlify.app

# Database Configuration
# Example for PostgreSQL (Railway provides this automatically)
DB_URL=postgresql://username:password@hostname:port/database

# Authentication
JWT_SECRET=your-super-secret-jwt-key-here-make-it-long-and-random

# API Keys (if you're using external services)
API_KEY_OPENAI=your-openai-api-key
API_KEY_THIRD_PARTY=your-third-party-api-key

# Optional: Redis URL for caching
REDIS_URL=redis://username:password@hostname:port

# Optional: Email service
EMAIL_SERVICE_API_KEY=your-email-service-key
EMAIL_FROM=noreply@yourdomain.com

# Optional: File upload service
CLOUDINARY_URL=cloudinary://api_key:api_secret@cloud_name

# Security
CORS_ORIGIN=https://your-site-name.netlify.app
SESSION_SECRET=another-long-random-secret-for-sessions
